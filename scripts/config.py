#replace the values of host, user, password, and database name (db) with your own values.
#ensure that you do not have a table name in the database you choose that is the same name as table_name

db = {
    'user' : 'root',
    'password' : 'toor',
    'host' : '127.0.0.1',
    'db' : 'bigdata'
}

table_name = 'crimes'
